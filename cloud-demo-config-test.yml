server:
  port: 9002
spring:
  application:
    name: clouddemo-config-client-test
eureka:
 client:
  service-url:
   defaultZone: http://eureka-test.com:7001/eureka/
info:
  application:
    name: ${spring.application.name}
    port: ${server.port}